CREATE TABLE if not exists overview  (
	origin VARCHAR(100),
	objectid VARCHAR(100),
	fieldid VARCHAR(100),
	lastExecTime VARCHAR(35),
	lastResultTime VARCHAR(35),
	state VARCHAR(35),
	target  VARCHAR(100),
	rootpos  VARCHAR(100),
	position  VARCHAR(100),
	comp VARCHAR(100)
);
