CREATE TABLE if not exists overview  (
	origin VARCHAR(100),
	objectid VARCHAR(100),
	diagid  VARCHAR(100),
	lastExecTime VARCHAR(35),
	lastResultTime VARCHAR(35),
	lastResult_HS VARCHAR(35),
	carnumber  VARCHAR(100),
	bogie  VARCHAR(100),
	axis  VARCHAR(100),
	comp VARCHAR(100)
);
